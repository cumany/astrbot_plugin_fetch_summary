{
    "enable_summary": {
        "description": "是否启用内容摘要",
        "type": "bool",
        "hint": "关闭后不再自动发送摘要",
        "default": true
    },
    "summary_prefix": {
        "description": "摘要提示词",
        "type": "string",
        "hint": "用于自动回复的摘要前缀",
        "default": "📝内容摘要："
    },
    "enable_llm_postprocess": {
        "description": "是否启用大模型处理",
        "type": "bool",
        "hint": "开启后会把摘要交给所选大模型按提示词进一步润色或翻译",
        "default": false
    },
    "llm_prompt_template": {
        "description": "大模型提示词",
        "type": "string",
        "hint": "可包含 {summary} 占位符，用于插入原始摘要",
        "default": "请将以下摘要润色成自然的中文群聊用语，保持原意：\n\n{summary}"
    },
    "provider": {
        "description": "大模型提供商",
        "type": "string",
        "default": "",
        "hint": "可选，留空则使用 AstrBot 当前默认模型",
        "_special": "select_provider"
    },
    "blacklist_groups": {
        "description": "群组黑名单",
        "type": "list",
        "hint": "不允许使用此插件的群组 ID 列表",
        "default": []
    },
    "trigger_keywords": {
        "description": "触发关键词",
        "type": "list",
        "hint": "仅当消息包含这些关键词时才触发处理，留空则处理所有 URL",
        "default": []
    },
    "timeout": {
        "description": "摘要请求超时（秒）",
        "type": "int",
        "hint": "单次摘要请求的最大等待时间",
        "default": 30
    },
    "max_retries": {
        "description": "摘要请求最大重试次数",
        "type": "int",
        "hint": "网络异常时的重试次数",
        "default": 2
    },
    "blacklist_keywords": {
        "description": "域名链接黑名单",
        "type": "list",
        "hint": "当URL包含这些关键词跳过处理",
        "default": [
            "baidu.com"
        ]
    }
}